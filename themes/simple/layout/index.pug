extends partial/layout.pug

mixin possibility(post)
  article
    section.meta !{list_categories(post.categories)} | #{date(post.date, 'DD/MM')}

    header
      h1
        a(href= url_for(post.path))= post.title

    section !{post.excerpt}

    footer
      p
        a(class='read-more' href= url_for(post.path)) Seguir leyendo

block content
  section.intro
    h1= site.data.simple.title
    .description !{site.data.simple.description}

  section.possibilities
    - page.posts.sort('date',-1).each(function (item) {
      +possibility(item)
    - })
